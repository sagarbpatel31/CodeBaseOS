# ingester

Multi-source ingestion: GitHub API (commits, PRs, issues, reviews), local git mirror, Slack (optional), Linear/Jira (optional).

**Key modules (Phase 1+):** `github.py`, `git_local.py`, `slack.py` (optional)

