# backend

FastAPI async query API + ingestion pipeline + webhook receiver.

**Endpoints (Phase 2+):** `/why`, `/five-whys`, `/summary`, `/provenance`, `/search-nl`, `/counterfactual`, `/handoff`, `/verify`, `/status`, `/repos`, `/ws`

Lane B owns this directory.
