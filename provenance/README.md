# provenance

OSS spinoff extracted from `/ingester` + `/graph` + `/synthesizer`. Standalone framework for bi-temporal code provenance.

**Target:** MIT-licensed, published to GitHub at hour 44–45.

Lane B populates this near end of hackathon.
