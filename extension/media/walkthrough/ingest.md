# Ingest this repo

Pull this repository's history — commits, PRs, issues, and decisions — into the
provenance graph.

Run **CodebaseOS: Ingest this repo**. The `owner/name` is auto-detected from your
git remote, so there's nothing to configure.

It builds a Merkle-verified, bi-temporal graph you can then ask questions about.
