# extension

VS Code extension (TypeScript strict, node 18 target).

**Features:** hover provider, code lens ("🧬 Why?"), webview panel, status bar (`CBOS: $X.XX / $5.00 · N nodes · Merkle ✓`), command palette commands.

**Must handle backend offline gracefully.**

Lane E owns this directory.
